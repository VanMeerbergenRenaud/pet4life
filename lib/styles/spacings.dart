
const kPadding = 16.0; // 16 pixels
const kHorizontalPadding = 16.0;
const kVerticalPadding = 16.0;

const kHorizontalPaddingS = kHorizontalPadding / 2; // 8
const kVerticalPaddingS = kVerticalPadding / 2;

const kHorizontalPaddingL = kHorizontalPadding * 2; // 32
const kVerticalPaddingL = kVerticalPadding * 2;

const kHorizontalPaddingXL = kHorizontalPaddingL * 2; // 64
const kVerticalPaddingXL = kVerticalPaddingL * 2;

const kHorizontalPaddingXXL = kHorizontalPaddingXL * 2; // 128
const kVerticalPaddingXXL = kVerticalPaddingXL * 2;
